import javax.swing.*;
import java.awt.*;

class AdminPage extends JPanel {
    public AdminPage() {
        setLayout(new BorderLayout());
        add(new JLabel("🔧 Admin"), BorderLayout.CENTER);
    }
}