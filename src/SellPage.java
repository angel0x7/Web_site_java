import javax.swing.*;
import java.awt.*;

class SellPage extends JPanel {
    public SellPage() {
        setLayout(new BorderLayout());
        add(new JLabel("🛒 Vendre un produit"), BorderLayout.CENTER);
    }
}